import 'package:flutter/material.dart';

class Testing extends StatelessWidget {
  const Testing({super.key, required this.d});
  final Map d ;

  @override
  Widget build(BuildContext context) {
    return  Scaffold();
  }
}
